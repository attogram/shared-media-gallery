{{ include('header.twig') }}
{{ include('menu.twig') }}
<div class="content">
    <form method="GET" style="float:right;">
        <input type="text" name="q" value="{{ query|e }}" size="16">
        <input type="submit" value="Search {{ media_count|default('0') }} Media">
    </form>
    {% for media in medias %}
        {{ include('thumb.media.twig') }}
    {% endfor %}
</div>
{{ include('footer.twig') }}
