{# shared-media-gallery  medias.twig  v0.0.1 #}
{{ include('header.twig') }}
{{ include('menu.twig') }}
<div class="content">
	{% for media in medias %}
		{{ include('thumb.media.twig') }}
	{% endfor %}
</div>
{{ include('footer.twig') }}
