{# shared-media-gallery  categories.twig  v0.0.1 #}
{{ include('header.twig') }}
{{ include('menu.twig') }}
<div class="content">
	<ul>
	{% for category in categories %}
		<li>{{ include('thumb.category.twig') }}</li>
	{% endfor %}
	</ul>
</div>
{{ include('footer.twig') }}
