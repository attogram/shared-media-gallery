{{ include('header.twig') }}
{{ include('menu.twig') }}
<div class="content">
	<h1>Sources Admin</h1>
	{% for source in sources %}
		<br />{{source.id}}: {{ source.title|e }} = 
		<a target="_blank" href="{{ source.host|e }}">{{ source.host|e }}</a>
	{% endfor %}
</div>
{{ include('footer.twig') }}
