{# shared-media-gallery  admin/source.twig  v0.0.1 #}
{{ include('header.twig') }}
{{ include('menu.twig') }}
<div class="content">
	<h1>Sources Admin</h1>
	<ul>
	{% for title, endpoint  in sources %}
		<li>{{ title }}: {{ endpoint }}</li>
	{% endfor %}
	</ul>
</div>
{{ include('footer.twig') }}
